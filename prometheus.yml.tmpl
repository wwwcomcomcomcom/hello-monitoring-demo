global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "springboot-app"
    metrics_path: "/hello-management/prometheus"
    static_configs:
      - targets: ["${SCRAPE_TARGET}"]

rule_files:
  - "/etc/prometheus/rules/*.yml"
