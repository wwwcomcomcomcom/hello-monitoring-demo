# Grafana Dockerfile
FROM grafana/grafana:latest

# (선택) 플러그인 자동 설치 예시
# ENV GF_INSTALL_PLUGINS=grafana-piechart-panel,grafana-clock-panel

EXPOSE 3000

# Grafana는 기본적으로 ENTRYPOINT와 CMD가 설정되어 있습니다.
# 필요시 환경변수나 설정파일을 추가로 COPY/ENV로 지정할 수 있습니다.
# 예시: 사용자 정의 grafana.ini 복사
# COPY grafana.ini /etc/grafana/grafana.ini
